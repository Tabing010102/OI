#include <cstdio>
#include <map>
#include <string>

using namespace std;

const int maxn = 1000+10;

int T, len;
char buf[maxn];
map<string, int> mf1, mf2, cnt;

int main() {
	scanf("%d", &T);
	while(T--) {
		scanf("%s", buf);
		len = strlen(buf);
		cnt = 0;
		while(cnt < len) 
	}
	return 0;
}
