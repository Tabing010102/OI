#include <bits/stdc++.h>

using namespace std;

int main() {
	srand(time(NULL));
	printf("%lld", (long long) (rand()*rand()*rand()*rand()*rand()*rand()*rand()*rand())%(long long)1e18+1);
	return 0;
}
