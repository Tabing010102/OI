#include <cstdio>
#include <cstring>

const int maxn = 100000 + 10;

int T, d[maxn], left[maxn], beigei, k, n;

int main() {
	scanf("%d", &T);
	while(T--) {
		memset(d, 0, sizeof(d));
		memset(left, 0, sizeof(left));
		scanf("%d%d", &n, &k);
	}
	return 0;
}
